# Development Dockerfile with hot reload for frontend
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Source code will be mounted via volume for hot reload
COPY . .

EXPOSE 3000

# Run Next.js dev server
CMD ["npm", "run", "dev"]
