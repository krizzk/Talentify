# Development Dockerfile with hot reload for backend
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Source code will be mounted via volume for hot reload
COPY . .

EXPOSE 4000

# Run with watch mode for development
CMD ["npm", "run", "start:dev"]
